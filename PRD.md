# 제품 요구사항 정의서 (PRD): 누구나 사용할 수 있는 범용 생산성 대시보드 (Landing Page)

## 1. 프로젝트 개요
브라우저를 열 때 가장 먼저 나타나 필요한 모든 환경을 자동으로 세팅해주는 범용 랜딩 페이지(대시보드)입니다. 누구나 웹을 통해 접속하여 자신만의 대시보드로 활용할 수 있도록 웹 호스팅 서비스(GitHub Pages, Vercel 등)를 통해 퍼블릭으로 배포할 수 있습니다. 사용자별 설정과 데이터 등은 브라우저의 `localStorage`를 활용하여 개별적으로 안전하게 보관되기 때문에, 별도의 회원가입이나 백엔드 서버 없이 순수 정적 페이지(HTML/CSS/JS)만으로도 다중 사용자를 완벽히 지원합니다.

## 2. 주요 기능 명세

### 2.1. 생산성 강화 기능
*   **그룹별 '원클릭' 세션 실행:** 
    *   사용자가 지정한 테마(예: 업무, 공부, 뉴스 서핑)별로 버튼을 클릭하면, 관련 사이트 3~5개가 `window.open`을 통해 새 탭으로 일괄 실행됩니다.
*   **간이 To-Do List (가장 중요한 3가지):** 
    *   오늘 꼭 해야 할 일 3가지만 적을 수 있는 위젯을 제공합니다. 
    *   데이터는 **Supabase 데이터베이스**에 연동되어 다른 디바이스 환경에서 접속해도 실시간으로 동기화되고 유지됩니다.
*   **뽀모도로 타이머:** 
    *   업무 집중을 위한 심플한 타이머 위젯을 상단에 배치합니다.
    *   기본 25분부터 시작하며, 사용자가 **+ / - 버튼을 통해 5분 단위로 자유롭게 조절**할 수 있습니다.

### 2.2. 정보 위젯 기능
*   **실시간 날씨 및 시계:** 
    *   OpenWeatherMap API를 연동하여 현재 위치의 날씨 정보를 표시합니다.
    *   직관적이고 미려한 디지털 또는 아날로그 시계를 함께 배치합니다.
*   **RSS 뉴스 피드 리더:** 
    *   사용자가 즐겨 찾는 뉴스 사이트나 블로그의 최신 글 제목을 리스트업하여 빠르게 정보를 훑어볼 수 있도록 합니다. (CORS 문제가 없는 공개 API 또는 RSS-to-JSON 컨버터 사용 예정)
*   **통합 검색바:** 
    *   구글, 네이버, 유튜브 등 자주 사용하는 검색 엔진을 선택하거나 단축키(Tab)로 변경한 후, 바로 검색할 수 있는 중앙 검색바를 제공합니다.

### 2.3. UX 및 디자인 중심 구현
*   **랜덤 배경화면 (Unsplash API):** 
    *   화면을 열 때마다 Unsplash API에서 고화질 풍경 및 영감을 주는 이미지를 랜덤으로 가져와 배경으로 설정합니다.
*   **다크 모드 / 라이트 모드 테마:** 
    *   시간에 맞게 자동으로 변경되거나, 사용자가 클릭 한 번으로 수동 변경할 수 있는 다크/라이트 모드 스위치를 추가합니다.
*   **드래그 앤 드롭 (Drag & Drop) 레이아웃:** 
    *   위젯 및 즐겨찾기 아이콘의 위치를 사용자 마음대로 옮길 수 있으며, 배치 정보는 **Supabase** 사용자 설정 DB에 저장되어 기기 간 동기화를 지원합니다.
*   **모던 UI 적용 (Glassmorphism & Micro-animations):** 
    *   반투명한 배경(유리 질감), 부드러운 그라데이션, 호버 액션 등 퀄리티 높은 프리미엄 디자인을 적용하여 단순 MVP 형태를 탈피합니다.

### 2.4. 기술적 요구사항 및 클라우드 연동
*   **클라우드 동기화 (Supabase):** 
    *   단순 로컬 저장을 넘어, 사용자 개별 환경 구성 및 할 일 데이터를 **Supabase (PostgreSQL DB & Auth)**를 통해 저장고 관리하여 사용자 맞춤형 클라우드 대시보드를 구축합니다.
*   **사용자 환경 맞춤화 설정 (Settings Page):** 
    *   초기 정적 `config.json` 매핑을 넘어, 사용자가 브라우저 안에서 직접 자신의 즐겨찾기 북마크, 사용할 검색 엔진, API 키(날씨 등), 그리고 PWA 테마 등을 편집할 수 있는 **설정 환경(Settings UI)**을 제공합니다.
*   **PWA (Progressive Web App):** 
    *   `manifest.json`과 Service Worker를 구성하여, 주소창이 없는 독립된 앱 형태로 설치 및 실행이 가능하도록 지원합니다.

## 3. 기술 스택 및 배포 환경
*   **Frontend**: HTML5 / CSS3 (Vanilla), JavaScript (ES6+), Vite(개발 및 빌드 최적화)
*   **Backend & DB**: **Supabase** (인증 관리 및 사용자 데이터, 위젯/설정 DB)
*   **배포 (Vercel)**: GitHub 원격 저장소를 기반으로 **Vercel** 에 연결하여 지속적 배포(CI/CD) 및 글로벌 엣지 네트워크 기반의 퍼블릭 호스팅을 구축.
*   **외부 연동 API**: OpenWeatherMap API(날씨), Unsplash Source API(배경 이미지)

## 4. 구조 및 제약 사항
*   데이터베이스나 사용자 관리는 전량 Supabase Client(BaaS)에 의존하여, 별도로 관리할 서버(Node, Python)를 구축하지 않습니다. (Serverless 기반)
*   사용자는 회원가입/로그인 세션을 통해 진입해야 하며, 로그인 전과 후의 상태를 구분합니다.
